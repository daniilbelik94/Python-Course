# Assignment 2:
"""
    create a function called separate() that accepts a string as an argument
    and returns a list containing each of the characters of
    the string separated as individual items in the list.

    Make sure to test the function.

    перевод:
    создайте функцию с именем separate(), которая принимает строку в качестве аргумента
    и возвращает список, содержащий каждый из символов
    строки, разделенных как отдельные элементы в списке.

    Обязательно протестируйте функцию.
"""
# Your Code Below:

def separate():
    for item in list:
        print(item)

    return list

list = ['a', 'afd', 'daniil', 1, 3 ]

print(separate())


def separate(string):
    return list(string)  # Разбиваем строку на список символов

# Тестируем функцию
print(separate("hello"))
print(separate("Python"))













































# Solution Below:

# def separate(str):
#     return list(str)
#
# print(separate("hello there"))